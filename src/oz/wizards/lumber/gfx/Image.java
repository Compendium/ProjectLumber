package oz.wizards.lumber.gfx;

import java.io.IOException;

public class Image {
	public int texId;
	//private Texture texture;
	
	public Image (String path) throws IOException
	{
		//TODO
		//texture = TextureLoader.getTexture("PNG", ResourceLoader.getResourceAsStream(path));
		//texId = texture.getTextureID();
		//System.out.println("Loaded image \"" + path + "\"" + (texture.hasAlpha() ? " with alpha." : " without alpha."));
	}
}
